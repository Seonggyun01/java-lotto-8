# java-lotto-precourse
## 기능 요구 사항

간단한 로또 발매기를 구현한다.

- 로또 번호의 숫자 범위는 1~45까지이다.
- 1개의 로또를 발행할 때 중복되지 않는 6개의 숫자를 뽑는다.
    - [ ]  Randoms.pickUniqueNumbersInRange(1, 45, 6)
    - [ ]  각 번호는 오름차순 정렬
    - [ ]  중복 불가
    - [ ]  다른 로또 번호와는 연관 없음(중복 가능)
- 당첨은 1등부터 5등까지 있다. 당첨 기준과 금액은 아래와 같다.
    - 1등: 6개 번호 일치/2,000,000,000원
    - 2등: 5개 번호 일치 + 보너스 번호 일치/ 30,000,000원
    - 3등: 5개 번호 일치/ 1,500,000원
    - 4등 : 4개 번호 일치/ 50,000원
    - 5등: 3개 번호 일치/ 5,000원
- 로또 구입 금액을 입력하면 구입 금액에 해당하는 만큼 로또를 발행한다.
    - [ ]  1,000원에 1장씩 로또를 발행한다.
- 로또 1장의 가격은 1,000원이다.
    - [ ]  unsignedInt 사용 범위 초과 “너무 큰 금액입니다.”
    - [ ]  1,000으로 나누어 떨어지지 않을 경우 “구입 금액은 1,000원 단위입니다.”
    - [ ]  숫자 앞뒤 공백은 제거하고 사용한다(사용자 친화적). trim()
    - [ ]  숫자가 아닌 문자가 들어올 경우 “구입 금액에 문자는 입력할 수 없습니다.”
    - [ ]  숫자에 0이 들어있는 경우 “1,000원 이상을 입력해주세요.”
    - [ ]  숫자에 음수가 들어있는 경우 “구입 금액에 음수는 입력할수 없습니다”
    - [ ]  숫자에 소수가 들어있는 경우 “구입 금액은 1,000원 단위 입니다.”
- 당첨 번호와 보너스 번호를 입력받는다.

  <당첨 번호>

    - [ ]  쉼표로 구분한다. → 유틸 클래스로 기능 분리
    - [ ]  숫자 앞뒤 공백은 제거하고 사용한다. trim()
    - [ ]  빈 토큰은 예외 처리 “숫자를 비워둘 수 없습니다. 6개 숫자를 입력해주세요.”
    - [ ]  숫자가 아닌 문자가 들어올 경우 “로또 번호에 문자는 입력할 수 없습니다.”
    - [ ]  숫자 앞에 0이 들어있는 경우 “0이 포함되어 있습니다”
    - [ ]  숫자에 음수가 들어있는 경우 “음수가 포함되어 있습니다”
    - [ ]  숫자에 소수가 들어있는 경우 “정수를 입력해주세요”
    - [ ]  중복된 숫자 검사 “당첨 번호에 중복인 숫자가 있습니다”
    - [ ]  6개가 아닌 경우 “당첨 번호는 정확히 6개여야 합니다.”
        - [ ]  입력된 숫자를 알려줄지?
        - [ ]  6개를 입력해달라고 통일?
        - [ ]  초과, 미만으로만 알려줄지?
    - [ ]  숫자가 1~45 범위를 벗어난 경우 “로또 번호는 1부터 45 사이의 숫자여야 합니다.”

  <보너스 번호>

    - [ ]  숫자 하나를 입력 받는다.
    - [ ]  숫자 앞뒤 공백은 제거하고 사용한다. trim()
    - [ ]  숫자가 아닌 문자가 들어올 경우 “보너스 번호에 문자는 입력할 수 없습니다.”
    - [ ]  숫자에 0이 들어있는 경우 “보너스 번호에 0을 입력할 수 없습니다.”
    - [ ]  숫자에 음수가 들어있는 경우 “보너스 번호에 음수는 입력할 수 없습니다.”
    - [ ]  숫자에 소수가 들어있는 경우 “정수를 입력해주세요”
    - [ ]  숫자가 1~45 범위를 벗어난 경우 “로또 번호는 1부터 45 사이의 숫자여야 합니다.”
    - [ ]  당첨 번호와 중복 “보너스 번호는 당첨 번호에 포함될 수 없습니다.”
- 사용자가 구매한 로또 번호와 당첨 번호를 비교하여 당첨 내역 및 수익률을 출력하고, 로또 게임을 종료한다.
    - [ ]  로또 번호를 당첨 번호와 비교한다. 당첨 개수 반환(int)(유틸이라 정적)
    - [ ]  보너스 번호를 당첨 번호와 비교한다. 포함 여부 반환(bool) (유틸이라 정적)
    - [ ]  같은 숫자의 개수에 따라 로또 등수를 반환한다. (정적)
    - [ ]  당첨 등수를 기록한다. Result(동적)
    - [ ]  당첨 등수를 결과로 수익금을 구한다. (유틸이라 정적)
    - [ ]  수익금과 구입 금액으로 수익률을 구한다. (유틸이라 정적)
        - [ ]  수익률은 소수점 둘째 자리에서 반올림한다(ex. 100.0%)
        - [ ]  수익률 = (당첨금 / 구입 금액) * 100
- 사용자가 잘못된 값을 입력할 경우 `IllegalArgumentException`을 발생시키고, “[ERROR]”로 시작하는 에러 메시지를 출력 후 그 부분부터 입력을 다시 받는다.
    - “[ERROR] 로또 번호는 1부터 45 사이의 숫자여야 합니다.”
    - `EXCEPTION`이 아닌 `IllegalArgumentException`, `IllegalStateException` 등과 같은 명확한 유형을 처리한다.
    - [ ]  잘못 입력 시 해당 입력 단계부터 다시 시작(이전 정상 입력은 유지) while이나 재귀함수로 구현..?


### 프로그래밍 요구 사항

- JDK 21 버전에서 실행 가능해야 한다.
- 프로그램 실행의 시작점은 `Application`의 `main()`이다.
- `build.gradle`파일은 변경할 수 없으며, **제공된 라이브러리 이외의 외부 라이브러리는 사용하지 않는다.**
- 프로그램 종료 시 `System.exit()`를 호출하지 않는다.
- 프로그래밍 요구 사항에서 달리 명시하지 않는 한 파일, 패키지 등의 이름을 바꾸거나 이동하지 않는다.
- 자바 코드 컨벤션을 지키면서 프로그래밍한다.
    - 기본적으로 [Java Style Guide](https://github.com/woowacourse/woowacourse-docs/blob/main/styleguide/java)를 원칙으로 한다.
- indent(인덴트, 들여쓰기) depth를 3이 넘지 않도록 구현한다. 2까지만 허용한다.
    - 예를 들어 while문 안에 if문이 있으면 들여쓰기는 2이다.
    - 힌트: indent(인덴트, 들여쓰기) depth를 줄이는 좋은 방법은 함수(또는 메서드)를 분리하면 된다.
- 3항 연산자를 쓰지 않는다.
- 함수(또는 메서드)가 한 가지 일만 하도록 최대한 작게 만들어라.
- 함수(또는 메서드)의 길이가 15라인을 넘어가지 않도록 구현한다.
    - 함수(또는 메서드)가 한 가지 일만 잘 하도록 구현한다.
- else 예약어를 쓰지 않는다.
    - else를 쓰지 말라고 하니 switch/case로 구현하는 경우가 있는데 switch/case도 허용하지 않는다.
    - 힌트: if 조건절에서 값을 return하는 방식으로 구현하면 else를 사용하지 않아도 된다.
- Java Enum을 적용하여 프로그램을 구현한다.
- 구현한 기능에 대한 단위 테스트를 작성한다. 단, UI(System.out, System.in, Scanner) 로직은 제외한다.
    - 단위 테스트 작성이 익숙하지 않다면 `LottoTest`를 참고하여 학습한 후 테스트를 작성한다.

### **Lotto 클래스**

- 제공된 `Lotto` 클래스를 사용하여 구현해야 한다.
- `Lotto`에 `numbers` 이외의 필드(인스턴스 변수)를 추가할 수 없다.
- `numbers`의 접근 제어자인 `private`은 변경할 수 없다.
- `Lotto`의 패키지를 변경할 수 있다.

```java
public class Lotto {
    private final List<Integer> numbers;

    public Lotto(List<Integer> numbers) {
        validate(numbers);
        this.numbers = numbers;
    }

    private void validate(List<Integer> numbers) {
        if (numbers.size() != 6) {
            throw new IllegalArgumentException("[ERROR] 로또 번호는 6개여야 합니다.");
        }
    }

    // TODO: 추가 기능 구현
}
```